{
	<% @roombookings.each do |roomid , Roombookings2| %>	
	"1": {
		"title": "Room_" <%= roomid %>,
		"schedule": [
			 <% @roombookings2.each do | book| %>	
			{
			"start": "<%= book.starttime.strftime("%H:%M") %>",
			"end": "<%= book.endtime.strftime("%H:%M") %>",
			"text": "Text Area",
			"data": {}
		},
		<% end %>
		]
          
  	
	}
	<% end %>
}
