{
	"1": {
	

		"title": "Room_<%= book.room_id %>",
		"schedule": [
			 <% @roombookings.each do |book| %>	
			{
			"start": "<%= book.starttime.strftime(%H:%M) %>",
			"end": "<%= convert_time(book.endtime.strftime(%H:%M)) %>",
			"text": "Text Area",
			"data": {}
		},
		<% end %>
		]
          
  	
	}
}
