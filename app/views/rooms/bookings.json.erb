{
	<% @roombookings.each do |roomid , book| %>	
	"1": {
		"title": "Room_" <%= roomid %>,
		"schedule": [
			 <% @roombookings.each do |roomid , book| %>	
			{
			"start": "<%= book.starttime.strftime("%H:%M") %>",
			"end": "<%= book.endtime.strftime("%H:%M") %>",
			"text": "Text Area",
			"data": {}
		},
		<% end %>
		]
          
  	
	}
	<% end %>
}
